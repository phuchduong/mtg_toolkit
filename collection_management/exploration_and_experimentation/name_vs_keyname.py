import os

# Find MTG JSON DB
# current_dir = os.path.dirname(os.path.realpath(__file__))
dir_path =  
card_db_name = "mtg_card_db.json"

# Ingress
# mtg_db = json.loads(open(file_name).read())
mtg_db = json.loads(

# view the difference between the key and name
def view_difference():
